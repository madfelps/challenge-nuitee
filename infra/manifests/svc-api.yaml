apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: nuitee-challenge
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
    - port: 80
      targetPort: 4000
      name: http
  type: ClusterIP


